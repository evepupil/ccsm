# 前端界面与主题

- **模块定位**：CCSM 桌面窗口、导航侧栏、会话工作区与明暗主题。
- **对应代码**：`src/App.tsx`、`src/components/`、`src/assets/`、`src/hooks/`、`src/lib/archive.ts`、`src/lib/selection.ts`、`src/lib/sessions.ts`、`src/lib/presentation.ts`、`src/lib/theme.ts`、`src/styles/`、`src/styles.css`、`scripts/generate-app-icon.ps1`、`src-tauri/icons/`、`src-tauri/tauri.conf.json`、`src-tauri/capabilities/default.json`；确认原型位于 `docs/prototypes/ccsm-ui-v3/`
- **所属里程碑**：[M2](../roadmap.md#m2)
- **当前状态**：进行中
- **最近更新时间**：2026-08-18

## 职责与边界

把后端 `SessionCatalog` 渲染成可筛选、可创建和可续接的桌面界面，管理来源切换、项目选择、搜索、排序、归档、最高权限、新会话、继续、分叉、复制和窗口控制。本模块不直接读取会话文件，所有真实数据经 `src/api.ts` 调用 Tauri 命令获得。

## 结构与数据流

```text
WindowTitlebar ── 当前来源 / 拖动 / 最小化 / 最大化 / 关闭

scanSessionCatalog (Tauri)
          │
          ▼
useSessionCatalog ── provider / catalog / selectedProjectId
          │
          ├──► ProjectSidebar
          │       ├── 来源切换 Tabs
          │       ├── 全局搜索 SearchField
          │       ├── 项目 ListBox
          │       └── 扫描按钮与最近扫描时间
          │
          └──► useSessionFilters ──► SessionToolbar
                         │
                         └──► SessionList ──► SessionItem
                                                   │
useSessionLauncher ◄───────────────────────────────┤
          ├──► resumeSession（继续 / 分叉）
          ├──► startNewSession（新会话）
          └──► clipboard + HeroUI Toast

useSessionArchive ──► localStorage（仅保存 provider + Session ID）
useSessionSelection ──► 选择模式与当前筛选结果的批量操作
```

`App.tsx` 只组装窗口和业务状态。`useSessionCatalog` 管理扫描、来源与项目选择，`useSessionFilters` 管理搜索、排序、归档和最高权限，`useSessionArchive` 管理 CCSM 本地归档状态，`useSessionSelection` 管理多选，`useSessionLauncher` 管理新会话、继续、分叉、复制与启动锁。`selectedProjectId` 和 CCSM 归档键通过 `localStorage` 记忆；`selectVisibleProjects` 与 `selectVisibleSessions` 作为纯函数计算左右两侧数据。

## 关键决策

1. 视觉方向采用 2026-08-10 用户确认的 v3 原型：暗色工具侧栏、浅色主工作区、紧凑控件和连续会话列表。
2. Tauri 主窗口关闭系统装饰，使用 38px 自定义标题栏；窗口按钮调用 `@tauri-apps/api/window`，对应能力在 `src-tauri/capabilities/default.json` 显式授权。
3. 标题栏只显示 CCSM、当前来源和窗口按钮。顶部不显示“本地索引已就绪”、状态圆点或同类说明；扫描反馈集中在侧栏底部。
4. 来源切换与搜索移入侧栏顶部。来源切换同时更新项目和会话，搜索在当前来源的全部项目中执行；从搜索结果选择项目会清空搜索并进入项目。
5. 会话展示改为按“今天 / 昨天 / 本周 / 更早”分组的连续列表。标题排序使用单独的“按标题排序”分组；不再使用后台表格和固定表头。
6. HeroUI 保留在 `Tabs`、`SearchField`、`ListBox`、`Button`、`Spinner` 和 Toast 等需要 React Aria 行为的控件上；窗口层、工具栏和会话行采用语义化 HTML，便于精确还原桌面布局。
7. 样式按职责拆为 `tokens.css`、`base.css`、`window.css`、`sidebar.css`、`sessions.css`，`styles.css` 只负责导入。明暗主题继续由 `useSystemTheme` 驱动 `.dark` class。
8. 所有固定控件和列表列使用稳定尺寸；1180×760 默认窗口显示独立活动时间，1000px 以下隐藏该列并保留核心元数据和操作。
9. 任一会话启动期间锁住全部继续与分叉按钮，防止桌面终端启动过程中重复提交。
10. 项目总数、会话数、扫描时间、权限风险和错误仍保留，因为它们直接影响操作判断；普通成功状态不占用主工作区。
11. Claude Code 与 Codex 的来源身份统一使用本地品牌素材，Tab 和会话行复用同一个 `ProviderLogo` 组件，不依赖运行时网络。应用图标以 Claude Code 图标为主体，右下角叠加 `>_` 会话管理徽标，和官方 Claude 客户端保持可区分性；应用 EXE、NSIS 安装器与卸载器统一使用该图标。
12. 归档只维护 CCSM 自己的本地状态，不修改 Claude JSONL、Codex SQLite 或 rollout 文件；归档键包含 provider 和 Session ID，避免两个来源的同 ID 会话互相影响。
13. 多选模式只作用当前筛选结果；选择状态是临时界面状态，切换来源、项目、搜索或归档筛选时清空。归档后的会话可通过“显示已归档”恢复，来源本身已归档的记录仍保持只读。
14. “新会话”只在项目视图中启用，使用当前来源、选中项目和当前最高权限设置；搜索状态禁用按钮，避免用户误以为会在搜索结果所属项目中创建。

## 当前实现

- **App.tsx**：组合 `WindowTitlebar`、`ProjectSidebar`、`SessionWorkspace` 与三个状态 hook，不再渲染旧 `AppHeader` 和 `StatusBar`。
- **WindowTitlebar.tsx**：品牌与当前来源、拖动区域、双击最大化、最小化、最大化和关闭按钮；普通浏览器预览中窗口动作安全跳过。
- **ProjectSidebar.tsx**：HeroUI `Tabs`、`SearchField`、`ListBox` 和扫描按钮；项目数量与来源、搜索、归档状态联动，底部显示总会话数和扫描时间。
- **SessionToolbar.tsx**：项目标题、路径、结果数、原生排序选择器、归档筛选、权限开关和多选批量操作；工具栏在窄窗口可换行，保持控件不重叠。
- **SessionWorkspace.tsx**：权限提醒、加载、错误、空数据、搜索标签和可滚动会话内容。
- **新会话入口**：会话标题右侧提供“新会话”按钮，复用当前来源和选中项目；启动中锁定按钮并通过 Toast 反馈终端启动结果。
- **SessionList.tsx / SessionItem.tsx**：按时间分组会话，展示来源图标、标题、项目上下文、分支、模型、用量、Session ID、活动时间、继续、分叉和归档操作；多选模式预留固定选择列。
- **归档与多选**：`useSessionArchive` 将 CCSM 归档键持久化到 WebView 本地存储，`useSessionSelection` 管理当前选择；工作区工具栏提供当前结果全选、批量归档和批量恢复。
- **ProviderLogo.tsx / assets**：集中映射 Claude Code 与 Codex 官方 Windows 客户端素材；来源 Tab、会话行和标题栏不再使用字母或通用占位图标。
- **应用图标**：`scripts/generate-app-icon.ps1` 从 Claude Code 素材生成 1024px Tauri 主图标与 64px 标题栏图标，再由 `pnpm icons:generate` 生成 Tauri 所需的 PNG、ICO 与 ICNS 产物。
- **styles/**：集中维护窗口、侧栏、会话和主题令牌；侧栏固定为开发工具风格，主区按系统明暗主题切换。
- **v3 HTML 原型**：`docs/prototypes/ccsm-ui-v3/index.html` 保留完整模拟数据和交互，用于快速确认设计方向；原型中的顶部索引状态也已同步移除。

## 验证方式

- `pnpm format:check`、`pnpm check`、`pnpm test` 全部通过，Vitest 为 5 个文件、19 项测试，覆盖归档键持久化、provider 隔离、批量状态切换和筛选隐藏。
- `pnpm build` 通过，Vite 转换 3479 个模块；JS 产物 459.06KB（gzip 140.64KB），不再触发 500KB 大包警告。
- `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings` 与 Rust 13 项测试全部通过。
- `pnpm tauri build --no-bundle` 在独立 `CARGO_TARGET_DIR` 中通过，验证无边框配置、窗口能力声明和 release 编译，不影响正在运行的旧 release 窗口。
- 真实 Tauri debug 窗口使用 242 条本机会话数据完成默认尺寸和 900×620 最小尺寸检查；标题栏无索引状态，侧栏不越界，工具栏和会话操作不重叠。
- HTML 原型在 1180×760 与 900×620 浏览器视口检查，并操作来源、项目、搜索、排序、归档、权限、复制、继续、分叉和刷新反馈。
- 2026-08-11 品牌素材回归：`pnpm icons:generate`、格式检查、TypeScript 严格检查、14 项前端测试、Vite 构建、Rust fmt/Clippy/11 项测试和全新 `CARGO_TARGET_DIR` 的 NSIS 构建通过；真实 Tauri 窗口分别检查 Claude 与 Codex Tab、会话行和标题栏图标，最终应用 EXE 与安装器提取图标一致。

## 待扩展项

- 最高权限开关保持 danger 状态和风险提醒；真实最高权限续接仍需用户主动触发。
- 900px 是当前最小窗宽；若以后支持更窄窗口，应增加可折叠侧栏。

## 改动历史

- 2026-07-28：引入 HeroUI v3 + Tailwind v4，拆分数据、筛选与启动状态 hook，接入系统明暗主题和 Toast。
- 2026-07-28：按第二轮原型改为左侧项目列表、右上来源切换，并修复项目选择、Switch 和窄窗口布局。
- 2026-08-10：优化旧 HeroUI 表格版本的控件层级、项目选中态和 900px 布局。
- 2026-08-10：用户确认 v3 桌面工具方向；正式前端迁移到无边框窗口、暗色导航侧栏和时间分组会话列表，移除顶部索引成功状态，并完成真实数据下的默认尺寸与最小尺寸回归。
- 2026-08-11：来源 Tab 与会话行接入 Claude Code、Codex 本地品牌素材；标题栏和 Tauri 应用图标改为 Claude Code 主图标叠加 `>_` 会话管理徽标。
- 2026-08-11：修正会话列表中的 Claude 图标透明留白，通过局部放大使其与 Codex 保持一致的视觉尺寸，不改变会话行容器尺寸。
- 2026-08-17：增加 CCSM 本地归档、归档恢复、会话多选和批量操作；保持 Claude/Codex 原始会话数据只读。
- 2026-08-18：在会话区右上角增加新会话入口，接入受控 Tauri 启动命令；搜索状态下禁用，避免项目上下文不明确。
- 2026-08-18：收窄会话多选区域；普通模式不再预留空选择列，多选模式将选择列收至 24px 并缩小列间距。
