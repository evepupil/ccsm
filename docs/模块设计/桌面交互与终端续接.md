# 桌面交互与终端续接

- **模块定位**：提供项目树、会话查找、受控终端续接和新会话创建的桌面操作界面。
- **对应代码**：`src/App.tsx`、`src/components/ProjectSidebar.tsx`、`src/components/SessionWorkspace.tsx`、`src-tauri/src/catalog.rs`、`src-tauri/src/launcher.rs`；M1 扩展见[多源启动与权限模式](多源启动与权限模式.md)
- **所属里程碑**：[M0](../roadmap.md#m0)
- **当前状态**：进行中
- **最近更新时间**：2026-08-18

## 职责与边界

React 界面负责浏览、搜索、筛选、排序、发起续接请求和创建新会话。Rust 启动器重新从本机索引解析 Session ID 或项目 ID，再创建新终端。前端不能提交任意目录、可执行文件或命令文本。

应用不接管 Claude 认证。新终端继承当前 Windows 用户环境，Claude CLI 未安装或未登录时在界面预先提示，实际登录和 Gateway 配置仍由 Claude CLI 处理。

## 结构与数据流

```text
启动 / 自动扫描（每 10 秒）
    │
    ├─ scan_session_catalog ──> 项目树 + 会话表格
    └─ get_cli_statuses ──────> 双 CLI 状态提示

点击“续接”或“分叉续接”
    │
    └─ resume_session(provider, sessionId, fork, highestPermissions)
           │
           ├─ UUID 校验
           ├─ 重新扫描并解析真实项目目录
           └─ Windows Terminal / PowerShell
                 └─ 固定的 Claude / Codex 续接命令

点击“新会话”
    │
    └─ start_new_session(provider, projectId, highestPermissions)
           │
           ├─ 普通项目：重新扫描并校验来源、项目 ID 和目录
           ├─ 临时会话：创建 Documents/CCSM/日期/new-chat(-N)
           └─ Windows Terminal / PowerShell
                 └─ 固定的 Claude / Codex 新会话命令

左侧“聊天 / 临时会话”
    ├─ 点击行：选择虚拟项目，右侧仍显示现有会话列表
    └─ 点击加号：直接创建独立目录并启动当前来源 CLI
```

## 关键决策

1. 主界面采用固定项目树和可滚动表格，面向大量会话的重复查找，不使用装饰性卡片布局。
2. 搜索跨项目匹配标题、项目路径、分支、模型和 Session ID；点击项目后回到该项目的完整会话列表。
3. “续接”保持原 Session ID，“分叉续接”通过独立图标按钮提供，避免无意制造重复会话。
4. Windows Terminal 不可用时，退回带独立控制台的 PowerShell。
5. 选中的项目只保存在 WebView 本地存储，不写入 Claude 数据。

## 当前实现

- 可展开项目树及最近六条会话预览。
- 跨项目搜索、按活动时间或标题排序、归档筛选。
- 会话来源、标题、时间、Session ID、消息数或 Token 数、文件大小、分支、模型/版本展示。
- Session ID 复制、原会话续接、分叉续接、项目目录丢失禁用态。
- 在当前选中项目中创建 Claude 或 Codex 新会话，搜索状态下按钮禁用并沿用当前来源和最高权限开关。
- 左侧提供独立的“聊天 / 临时会话”入口；临时目录按日期和 `new-chat(-N)` 命名，持久保留并聚合展示。
- 临时会话仍使用普通会话列表的续接、分叉、归档、复制 ID 和来源标识；列表中显示真实工作目录，悬浮可查看完整路径。
- 自动扫描、空结果、错误、启动中和操作提示状态。
- 系统浅色/深色主题与 900 × 620 最小窗口约束。

## 验证方式

前端单测覆盖相对时间、文件大小、搜索规范化、跨项目搜索、归档过滤、provider 过滤、最高权限警告和排序；本次实现后共 21 项通过。Rust 单测覆盖临时项目聚合和编号目录创建；本次实现后共 16 项通过。TypeScript 开启严格模式、未使用变量检查、索引访问检查和可选属性精确检查。

本次通过 `pnpm format:check`、TypeScript 检查、21 项前端单测、Rust 格式检查、16 项 Rust 单测、Clippy、`pnpm build` 和 `pnpm tauri:build`；已生成 x64 EXE、MSI 和 NSIS 包，并启动 release EXE 确认窗口响应。仍需人工点击临时会话加号，确认本机 CLI 在新目录中实际启动。

## 待扩展项

- 会话收藏、标签和快捷过滤。
- 键盘导航与命令面板。
- 自定义 Claude CLI 可执行文件或安全的本地包装脚本。
- 自动更新和代码签名。

## 改动历史

- 2026-07-27：建立 M0 项目树、会话表格、全局搜索、CLI 检测和安全续接；完成 8 项前端单测与 Tauri release 构建。
- 2026-07-27：接入双来源筛选、双 CLI 状态、provider 标识和最高权限警告；具体命令策略归档到 M1 模块文档。
- 2026-08-18：增加按当前项目创建全新 Claude/Codex 会话的入口，后端校验项目 ID 后复用受控终端启动流程。
- 2026-08-18：移除手动刷新入口，改为前端每 10 秒自动扫描；扫描重叠时跳过触发，组件卸载时清理定时器。
- 2026-08-18：增加“聊天 / 临时会话”虚拟入口，创建持久临时目录并把其中的双来源会话合并到同一列表。
- 2026-08-18：将临时会话入口收为单一卡片；默认显示会话数，悬浮、键盘聚焦或启动中在同一位置显示新建与加载状态。
